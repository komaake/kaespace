import React from "react";
import { Page } from "../types";

const Gallery: Page = () => {
  return <div>Mint</div>;
};

Gallery.id = "gallery";
Gallery.title = "Gallery";
Gallery.framework = "bootstrap";

export default Gallery;
