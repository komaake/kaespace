import React from "react";
import { Page } from "../types";

const Index: Page = () => {
  return <></>;
};

Index.id = "index";
Index.route = "/";
Index.title = "Home";
Index.framework = "bootstrap";

export default Index;
