import React from "react";
import { Page } from "../types";

const Launchpad: Page = () => {
  return <div>Mint</div>;
};

Launchpad.id = "launchpad";
Launchpad.title = "Launchpad";
Launchpad.framework = "bootstrap";

export default Launchpad;
