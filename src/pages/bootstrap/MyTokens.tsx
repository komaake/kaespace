import React from "react";
import { Page } from "../types";

const MyTokens: Page = () => {
  return <div>Mint</div>;
};

MyTokens.id = "my-tokens";
MyTokens.title = "MyTokens";
MyTokens.framework = "bootstrap";

export default MyTokens;
