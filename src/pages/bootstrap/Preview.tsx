import React from "react";
import { Page } from "../types";

const Preview: Page = () => {
  return <div>Mint</div>;
};

Preview.id = "preview";
Preview.title = "Preview";
Preview.framework = "bootstrap";

export default Preview;
